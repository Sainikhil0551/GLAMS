pipeline {
    agent any
      stages {
          stage ("Build") {
              steps {
              sh "docker build -t sainikhil1999/myapp:latest ."
              }
             }
           }
   }
